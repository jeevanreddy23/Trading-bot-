# Copy to .env and `export $(grep -v '^#' .env | xargs)` before a LIVE run.
# NEVER commit real keys. Create exchange keys WITHOUT withdrawal permission.

# --- live crypto (venue must match execution.live.crypto_venue) ---
KRAKEN_API_KEY=
KRAKEN_API_SECRET=

# --- live / practice FX via OANDA ---
OANDA_API_TOKEN=
OANDA_ACCOUNT_ID=

# --- final interlock: live orders refuse to send without this exact value ---
# LIVE_TRADING_ACK=I_UNDERSTAND_THE_RISKS
